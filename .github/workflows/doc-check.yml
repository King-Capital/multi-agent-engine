name: Documentation Check

on:
  pull_request:
    paths:
      - 'engine/**'
      - 'dashboard/**'

permissions:
  contents: read
  pull-requests: write

jobs:
  check-docs:
    uses: King-Capital/king-capital/.github/workflows/doc-check.yml@main
